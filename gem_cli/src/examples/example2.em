fn main() {
	//For loops in EmeraldScript look very similar to many other languages, and function the same as well
	for(i = 0; i < 10; i++){
		//EmeraldScript now has two printing keywords, one with a trailing new line chatacer and one without
		print "Guess what: ";
		println "EmeraldScript rules!";
	}
}